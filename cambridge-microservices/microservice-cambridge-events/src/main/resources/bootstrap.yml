spring:
   profiles:
      active: test
   application:
      name: cambridgeevents
   cloud:
      config:
         uri: ${CONFIG_URI:http://localhost:8888}
         enabled: true
         profile: test